import Cat;
import Dog;
import Parrot;
//I'm Batman! But only on Tuesdays!
public class User {

    public User(String name = "JoMama"; String mood = "happy"; int action = 0){
        User.name = name;
        User.mood = mood;
        User.action = action;
    }
    private action(User u){
        //get input to get action (what pet do you want to interact with? what action do you want to take with that pet? etc.)
        //actions could be related to the cat dog parrot functions

        //set action to 0 after it is done
    }
}